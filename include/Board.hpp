#include <vector>

class Board
{
private:

    //Variables
    //std::vector<std::vector<int>> boardVector;

    //Functions

public:

    std::vector<std::vector<int>> boardVector;


    //functions
    void initBoard(int w, int h);


    Board();
    ~Board();
};
